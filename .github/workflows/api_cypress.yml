name: Cypress Tests

on:
  push:
    branches:
      - main
      - develop

  pull_request:
    branches:
      - main
      - develop

jobs:

  tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run Cypress
        run: npm test

      - name: List generated reports
        if: always()
        run: |
          echo "Arquivos gerados:"
          find cypress/results -type f 2>/dev/null || true

      - name: Upload Cypress JSON Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-json-results
          path: cypress/results/
          if-no-files-found: error


  report:
    runs-on: ubuntu-latest

    needs: tests

    if: always()

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Download Cypress JSON Results
        uses: actions/download-artifact@v4
        with:
          name: cypress-json-results
          path: cypress/results/

      - name: List downloaded reports
        run: |
          echo "JSONs baixados:"
          find cypress/results -type f

      - name: Merge Mochawesome JSONs
        run: |
          npx mochawesome-merge "cypress/results/*.json" \
            -o cypress/results/mochawesome.json

      - name: Generate HTML Report
        run: |
          npx marge cypress/results/mochawesome.json \
            -f cypress-report \
            -o cypress/reports

      - name: Upload HTML Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-report
          path: cypress/reports/
          if-no-files-found: error